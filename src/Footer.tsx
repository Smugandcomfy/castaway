/// Persistent line at the bottom of every non-splash page. Small, muted,
/// italic serif — same tone as the methodology on the Oracle header.

export function Footer() {
  return (
    <footer className="ca-footer">
      <p className="ca-footer-line">
        Cast your intentions with verifiable magic.
      </p>
    </footer>
  );
}
